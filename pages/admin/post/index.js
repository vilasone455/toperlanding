import React from 'react'
import BlogComponent from '../../../components/BlogComponent'

export default function index() {
    return (
        <BlogComponent isEdit={false} slug={""}>
            
        </BlogComponent>
    )
}
